package main

import "fmt"
func CountIf (f func(string) bool, tab []string ) int {
	count := 0
	if i:= 0 ; i < len(tab), i++ {
		return count ++
	} 

	if f(a(tab[i])) {
		return false
	} 
}

func main(){
	fmt.Println(CountIf)
}